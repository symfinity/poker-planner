# Deprecated: ux-blocks-core prepends AssetMapper path via bundle extension (symfony-ux).
# Kept for reference during dogfood migration.
framework:
    asset_mapper:
        paths:
            '%kernel.project_dir%/vendor/symfinity/ux-blocks-core/assets': ux-blocks-core

stimulus:
    controller_paths:
        - '%kernel.project_dir%/assets/controllers'
